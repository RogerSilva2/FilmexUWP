<Page
    x:Class="FilmexUWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FilmexUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ProgressBar x:ConnectionId='1' IsIndeterminate="true" Name="Progresso" Visibility="Visible" />
        <ListBox x:ConnectionId='2' Name="Listagem" ItemsSource="{Binding}">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel VirtualizingStackPanel.VirtualizationMode="Recycling"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Image Width="90" Height="90">
                            <Image.Source>
                                <BitmapImage UriSource="{Binding Poster_Path}" />
                            </Image.Source>
                        </Image>
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="{Binding Title}" TextWrapping="Wrap" Width="Auto" />
                            <StackPanel Orientation="Horizontal">
                                <TextBlock>Popularidade</TextBlock>
                                <TextBlock Text="{Binding Popularity}" TextWrapping="Wrap" Width="Auto" />
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>

